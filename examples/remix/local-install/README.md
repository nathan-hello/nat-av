# Local Package Install Example

This example verifies the package artifacts rather than workspace source
aliases. The package tarballs are generated by the workspace pack task and
installed through local `file:` dependencies before running the typecheck.
